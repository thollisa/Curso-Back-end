function salario(){
   salario= horas*reais
   return (salario)
}
var reais= prompt(`Quantos reais voce por hora? ${reais}`);
var horas= prompt (`Quantas horas trabalhadas? ${horas}`);
 salario (`Salario igual a : ${salario}`)